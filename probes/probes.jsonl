{"code": "from flask import send_file\n\napp = Flask(__name__)\n\n@app.route(\"/<path:filename>\")\ndef download_file(filename):\n  # ruleid:avoid_send_file_without_path_sanitization\n  return send_file(filename)\n\ndef download_not_flask_route(filename):\n  # ok:avoid_send_file_without_path_sanitization\n  return send_file(filename)\n", "file": "python/flask/security/secure-static-file-serve.py", "id": "secure-static-file-serve_L8", "label": "FLAG", "line": 8, "spurious": false}
{"code": "from flask import send_file\n\napp = Flask(__name__)\n\n@app.route(\"/<path:filename>\")\ndef download_file(filename):\n  # ruleid:avoid_send_file_without_path_sanitization\n  return send_file(filename)\n\ndef download_not_flask_route(filename):\n  # ok:avoid_send_file_without_path_sanitization\n  return send_file(filename)\n", "file": "python/flask/security/secure-static-file-serve.py", "id": "secure-static-file-serve_L12", "label": "SUPPRESS", "line": 12, "spurious": false}
